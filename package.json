{
  "name": "ai-agents-blast",
  "private": true,
  "description": "Root scripts delegate to the Next.js app in web/",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "install:web": "npm install --prefix web",
    "vercel:pull": "npm run vercel:pull --prefix web",
    "vercel:link": "npm run vercel:link --prefix web"
  }
}
